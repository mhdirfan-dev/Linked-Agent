// config.production.js — Deployed on Vercel
// The real key is stored in Vercel Environment Variables (never in code).
// This file is committed safely — it contains only a placeholder.
// Vercel's build process replaces GROQ_API_KEY via the dashboard setting.

window.__ENV__ = {
  GROQ_API_KEY: "PASTE_YOUR_GROQ_KEY_HERE"
};
